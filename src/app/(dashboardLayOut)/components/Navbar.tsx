"use client";
const Navbar = () => {
  return (
    <header className="bg-white shadow px-6 py-4 flex items-center justify-between">
      <h1 className="text-xl font-semibold">Dashboard</h1>
      <div>{/* Profile, Logout button etc. */}</div>
    </header>
  );
};

export default Navbar;
